file(GLOB LIBSRC *.f90)
add_library(ORION ${LIBSRC})
if (USE_TECIO)
  add_compile_definitions(TECIO)
  if (TECIO_PREBUILD)
    target_link_libraries(ORION ${TECIO} ${LINKLIBS})
  else()
    target_link_libraries(ORION tecio ${LINKLIBS})
  endif()
endif()
